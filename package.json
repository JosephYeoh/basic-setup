{
	"private": true,
	"name": "basic-setup",
	"workspaces": [
		"packages/*"
	],
	"scripts": {
		"start": "concurrently \"yarn common gql -w\" \"yarn server start\"  \"yarn client start\"",
		"server": "cd packages/server && yarn",
		"client": "cd packages/client && yarn",
		"common": "cd packages/common && yarn"

	},
	"dependencies": {
		"concurrently": "^5.2.0"
	},
	"husky": {
    "hooks": {
      "pre-push": "yarn client format && yarn client eslint && yarn server format && yarn server eslint"
    }
  }
}
