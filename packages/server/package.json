{
  "name": "joseph-server",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "tsnode": "ts-node -r tsconfig-paths/register",
    "start": "nodemon --watch \"./src/**/*.ts\" --exec \"yarn tsnode -T\" ./src/server.ts",
    "lint-fix": "yarn tslint --project ./ --fix"
  },
  "dependencies": {
    "@types/glob": "^7.1.3",
    "apollo-server": "^2.16.0",
    "glob": "^7.1.6",
    "graphql": "^15.3.0",
    "tsconfig-paths": "^3.9.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.4",
    "ts-node": "^8.10.2"
  }
}
